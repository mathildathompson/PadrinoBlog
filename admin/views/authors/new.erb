<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:authors, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:authors, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :author, url(:authors, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'authors/form', :locals => { :f => f } %>
 <% end %>
</div>
